{
  "framework": "vite",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --dir template-aurora-ds build",
  "outputDirectory": "template-aurora-ds/dist"
}
