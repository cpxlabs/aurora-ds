{/* @ts-expect-error - Storybook MDX */}
import { Meta, Title, Subtitle, Description, Canvas, Source } from '@storybook/addon-docs/blocks';
import * as GettingStartedStories from './GettingStarted.stories';

<Meta of={GettingStartedStories} />

<Title>Aurora DS</Title>
<Subtitle>Componentes React com TypeScript, Vite, Radix UI e CSS Modules</Subtitle>

<Description>
Este Storybook documenta os componentes públicos do Aurora DS.

### Instalação

```bash
pnpm add @cpxlabs/aurora-ds
```

### Importação

> Importe os componentes pelo pacote raiz e o tema uma vez a partir de `theme.css`.

```ts
import "@cpxlabs/aurora-ds/theme.css";
import { Button, Dialog } from '@cpxlabs/aurora-ds';
```

### Modo escuro

Use o atributo `data-theme="dark"` no `<html>` ou em um wrapper:

```html
<html data-theme="dark"></html>
```

No Storybook, você pode trocar o tema pelo controle **Tema** na toolbar.
</Description>

## Exemplo rápido

<Canvas of={GettingStartedStories.QuickExample} />

<Source
  language="tsx"
  code={`import "@cpxlabs/aurora-ds/theme.css";
import { Button } from '@cpxlabs/aurora-ds';

export function Exemplo() {
  return (
    <>
      <Button>Primário</Button>
      <Button variant="ghost">Secundário</Button>
    </>
  );
}`}
/>
