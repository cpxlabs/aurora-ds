{/* @ts-expect-error - Storybook MDX */}
import { Meta, Title, Subtitle, Description, Canvas, Source } from '@storybook/addon-docs/blocks';
import * as DialogStories from './Dialog.stories';

<Meta of={DialogStories} />

<Title>Dialog</Title>
<Subtitle>Modal acessível construído sobre os primitivos do Radix UI</Subtitle>

<Description>
`Dialog` encapsula os primitivos de diálogo do Radix com os estilos do Aurora DS.

- Overlay e conteúdo são estilizados com CSS Modules
- Gerenciamento de foco e portal vêm do Radix
- `DialogContent` inclui um botão de fechar padrão
</Description>

## Exemplo (composição)

<Canvas of={DialogStories.Basic} />

<Source
  language="tsx"
  code={`import {
  Dialog, DialogTrigger, DialogContent, DialogTitle, DialogDescription,
  Button
} from '@cpxlabs/aurora-ds';

export function DialogoDeConfirmacao() {
  return (
    <Dialog>
      <DialogTrigger asChild>
        <Button>Abrir</Button>
      </DialogTrigger>

      <DialogContent>
        <DialogTitle>Confirmação</DialogTitle>
        <DialogDescription>Tem certeza?</DialogDescription>
        <div style={{ display: 'flex', gap: 8, justifyContent: 'flex-end' }}>
          <Button variant="ghost">Cancelar</Button>
          <Button>Confirmar</Button>
        </div>
      </DialogContent>
    </Dialog>
  );
}`}
/>
