{/* @ts-expect-error - Storybook MDX */}
import { Meta, Title, Subtitle, Description, Primary, Controls, Canvas, Source } from '@storybook/addon-docs/blocks';
import * as ButtonStories from './Button.stories';

<Meta of={ButtonStories} />

<Title>Button</Title>
<Subtitle>Botão de ação principal com variantes solid e ghost</Subtitle>

<Description>
`Button` é um componente de ação leve construído sobre o elemento nativo `<button>`.

- Variantes: `solid` e `ghost`
- Acessibilidade: os estilos de `:focus-visible` usam o token compartilhado `--ui-focus`
</Description>

## Playground

<Primary />
<Controls />

## Exemplos

<Canvas of={ButtonStories.Solid} />
<Canvas of={ButtonStories.Ghost} />
<Canvas of={ButtonStories.Disabled} />

<Source
  language="tsx"
  code={`import { Button } from '@cpxlabs/aurora-ds';

export function Acoes() {
  return (
    <div style={{ display: 'flex', gap: 8 }}>
      <Button variant="ghost">Cancelar</Button>
      <Button>Salvar</Button>
    </div>
  );
}`}
/>
